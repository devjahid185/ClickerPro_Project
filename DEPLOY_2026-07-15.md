# Deploy — Heaven feedback 2026-07-15 batch (commit 1755b2f)

## 1) Backend (cPanel Terminal — server-এ চালাতে হবে)

কোনো নতুন migration নেই — শুধু ৪টা controller বদলেছে। তবু নিরাপত্তার জন্য migrate রাখা হলো।

```bash
cd ~/clickerpro
git pull
php artisan migrate --force
php artisan config:cache
php artisan route:cache
```

পরিবর্তিত controller: AuthController (login restore + dup-phone), ProfileController
(public_booking_token backfill + role validation), AccountController (delete →
token purge + 7-day grace), BookingController (freelancer own-booking status)।

> `git pull` "local changes would be overwritten" দিলে: `git stash push -u -m "server-edits"` তারপর আবার pull।

## 2) Web (আমি build করেছি — cPanel-এ upload)

zip: `web_app_graphy7_deploy.zip` (~20MB, প্রোডাকশন API `https://api.deyalghori.com` bake করা, SPA `.htaccess` ভেতরে আছে)।

cPanel File Manager-এ home-এ upload → Terminal:

```bash
# পুরনোটা backup + খালি করে extract
mv ~/clickerpro/web_app_public ~/clickerpro/web_app_public_old_20260715 2>/dev/null
mkdir -p ~/clickerpro/web_app_public
unzip -o ~/web_app_graphy7_deploy.zip -d ~/clickerpro/web_app_public
```

> docroot = `~/clickerpro/web_app_public/` (public_html নয়)।
> verify: browser **incognito**-তে খুলুন (Flutter PWA service worker পুরনো cache ধরে রাখে)।

## 3) APK

`Graphy7.apk` (clickerpro flavor release) — ফোনে install করুন। একই keystore
(`clicker_pro.jks`) দিয়ে signed, তাই OTA update কাজ করবে।

## Verify checklist (deploy-এর পর)
- Freelancer login → booking detail-এ assignments/history/invoice নেই, শুধু Share
- Freelancer booking → Move to Complete + Cancel কাজ করে
- Web dashboard KPI card (Today/Upcoming/Total/Delivered) tap → সঠিক scoped list
- নতুন registration → web সাথে সাথে ঠিক layout (refresh ছাড়াই)
- একই phone/email দিয়ে ২য় registration block
- Web account delete → mobile অটো logout; ৭ দিনে relogin = restore
- Chat option কোথাও নেই; Office Staff role register-এ নেই
