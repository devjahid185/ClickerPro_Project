<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ (API 31+) launch theme.
     From API 31 the OS ALWAYS shows its own SplashScreen (a themed window with
     a centered app icon) on cold start — the old `windowBackground` drawable is
     ignored. Heaven wants that icon-screen gone, so we neutralise it:
       • windowSplashScreenBackground = BLACK, matching the Dart SplashScreen's
         black top edge, so the handoff is seamless (no flash / no jump).
       • windowSplashScreenAnimatedIcon = a transparent shape, so no app icon
         is drawn — the user just sees black, then the real sunset splash.
     The window is dismissed the moment Flutter draws its first frame. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_transparent</item>
        <item name="android:windowBackground">@color/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/launch_background</item>
    </style>
</resources>
